const a = 8;
const b = 5;

console.log(a > b ? 'taip' : 'ne');

const age = 18;
const ageLimit = 18;

const status = age >= ageLimit ? 'galiu' : 'dar per jaunas'
console.log(status);

const metai = 20;
const limitas = 21;

const arGalima = metai >= limitas ? 'galiu' : 'negaliu';
console.log(arGalima);

const nr1 = 67;
const nr2 = 66;

const ternary = nr1 > nr2 ? 'galima' : 'negalima';
console.log(ternary);